machine:
  java:
    version: openjdk8
  services:
    - mongod
dependencies:
  override:
    - mongo collector  --eval 'db.addUser("admin", "admin");'
    - mvn package -DskipTests
test:
  override:
    - mvn test
